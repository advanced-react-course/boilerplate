import { Meta, StoryObj } from '@storybook/react';
import { {{pascalCase name}}, {{pascalCase name}}Properties } from '.';

export default {
  title: 'Components / {{pascalCase name}}',
  component: {{pascalCase name}},
} as Meta<{{pascalCase name}}Properties>;

export const Default: StoryObj<{{pascalCase name}}Properties> = {
  parameters: {
    jest: ['{{pascalCase name}}/test.tsx'],
  },
};
