import { screen } from '@testing-library/react';
import { renderWithTheme } from 'utils/tests/helpers';
import {{pascalCase name}}Template from '.';

describe('<{{pascalCase name}}Template />', () => {
  it('should render the {{pascalCase name}}Template', () => {
    renderWithTheme(<{{pascalCase name}}Template title="{{pascalCase name}}" />);

    expect(
      screen.getByRole('heading', { name: /{{pascalCase name}}/i }),
    ).toBeInTheDocument();
  });
});
