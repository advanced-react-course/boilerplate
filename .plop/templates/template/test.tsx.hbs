import { screen } from '@testing-library/react';
import { renderWithTheme } from '~utils/tests/helpers';
import { {{pascalCase name}}PageTemplate } from '.';

describe('<{{pascalCase name}}PageTemplate />', () => {
  it('should render {{pascalCase name}}PageTemplate component', () => {
    renderWithTheme(<{{pascalCase name}}PageTemplate title="{{titleCase name}}" />);

    expect(screen.getByTestId('{{dashCase name}}-page-template')).toBeInTheDocument();

    expect(screen.getByTestId('title')).toBeInTheDocument();

    expect(screen.getByTestId('title')).toHaveTextContent('{{titleCase name}}');
  });
});
